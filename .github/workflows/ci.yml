name: ci

on:
  push:
    branches: [main]
  pull_request:

jobs:
  test:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - name: Parse and unit tests
        shell: powershell
        run: |
          powershell -NoProfile -ExecutionPolicy Bypass -File .\tests\Invoke-ParseCheck.ps1
          powershell -NoProfile -ExecutionPolicy Bypass -File .\tests\ConvertTo-ProfileKey.Tests.ps1
          powershell -NoProfile -ExecutionPolicy Bypass -File .\tests\AuthSwap.Tests.ps1
